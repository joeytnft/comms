// Web stub — expo-file-system is native-only
export const EncodingType = { Base64: 'base64' as const, UTF8: 'utf8' as const };
export async function readAsStringAsync(_uri: string, _opts?: { encoding?: string }): Promise<string> {
  return '';
}
